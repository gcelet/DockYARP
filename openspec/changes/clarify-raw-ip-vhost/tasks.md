## 1. Lock behavior (AG-RP)
- [x] 1.1 Unit test (`tests/DockYarp.Core.Tests`): a route with an IPv4-literal host matches a request to that
      IP

## 2. Spec & docs (AG-RP)
- [x] 2.1 Clarify in the `proxy-routing` spec that an exact host match supports a bare IPv4 `VIRTUAL_HOST`
- [x] 2.2 Note in `docs/labels-reference.md` that `VIRTUAL_HOST` may be a bare IPv4 address (IPv6 literal is a
      caveat)
