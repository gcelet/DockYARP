## 1. Add the diagnostics step (AG-DEP)

- [x] 1.1 Add "Upload E2E diagnostics" to `renovate-gate.yml`'s `full-gate` job, mirroring `image.yml`'s
      existing step, and verify the workflow YAML is still valid (`gh workflow view` or a syntax check).
