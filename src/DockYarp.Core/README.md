# DockYarp.Core
Contains internal models, stores, and abstractions used across DockYarp.

Agents will generate:
- Route model
- Cluster model
- TLS model
- Stores (atomic snapshot stores)
- Interfaces for discovery, routing, TLS, security
