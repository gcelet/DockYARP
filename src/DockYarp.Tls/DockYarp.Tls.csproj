<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.IO.Abstractions" />
    <PackageReference Include="Portable.BouncyCastle" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DockYarp.Core\DockYarp.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Exposes the internal Acme.* JWS/HTTP-client helpers and AcmeClient's chain-assembly logic for unit
         testing — the rest of AcmeClient itself requires a live ACME network round-trip, exercised via e2e only. -->
    <InternalsVisibleTo Include="DockYarp.Tls.Tests" />
  </ItemGroup>

</Project>
