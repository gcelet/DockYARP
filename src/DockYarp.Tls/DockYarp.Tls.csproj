<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Certes" />
    <PackageReference Include="System.IO.Abstractions" />
    <PackageReference Include="Portable.BouncyCastle" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DockYarp.Core\DockYarp.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- CertesAcmeClient's chain-assembly logic is otherwise untestable at the unit level (the rest of the
         class requires a live ACME network round-trip) — this exposes only that pure, deterministic piece. -->
    <InternalsVisibleTo Include="DockYarp.Tls.Tests" />
  </ItemGroup>

</Project>
