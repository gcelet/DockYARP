<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BCrypt.Net-Next" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DockYarp.Core\DockYarp.Core.csproj" />
    <!-- ClientCertificateMiddleware reuses ClientCertificateValidator (CA-chain + CRL check) rather than
         re-implementing chain/CRL validation here; Tls only depends on Core, so this introduces no cycle. -->
    <ProjectReference Include="..\DockYarp.Tls\DockYarp.Tls.csproj" />
  </ItemGroup>

</Project>
