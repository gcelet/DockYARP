{
  "name": "dockyarp-docs",
  "version": "0.1.0",
  "private": true,
  "description": "DockYARP documentation site (Hugo + Docsy)",
  "scripts": {
    "get:submodule": "git submodule update --init --depth 1",
    "prepare": "npm run get:submodule",
    "serve": "hugo serve",
    "build": "hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "hugo-extended": "^0.164.0",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0"
  }
}
